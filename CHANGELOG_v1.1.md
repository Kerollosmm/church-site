# سجل التغييرات v1.1.0 — قرارات المصالحة قبل البناء
**التاريخ:** 2026-09-14 | **النطاق:** توحيد الحزمة التوثيقية بعد المراجعة المتقاطعة، وإضافة المواصفات المفقودة.

## 1. القرارات الحاكمة (Resolving Decisions)

| # | التعارض/الخلل المكتشف | القرار المعتمد v1.1 | الملفات المعدلة |
| :- | :--- | :--- | :--- |
| 1 | إصدار الإطار: Next.js 14 (README/الموجه) مقابل 15 (ADR-0001)، وحزمة مهجورة `@supabase/auth-helpers-nextjs` | **Next.js 15 App Router** + الحزمة الرسمية **`@supabase/ssr`** فقط | README، AI Prompt، BACKEND §1/§7 |
| 2 | الاجتماعات: "10 قطاعات" نصاً مقابل 11 صفاً فعلياً في الجداول والمسارات | **11 اجتماعاً** — القائمة القانونية تشمل `widows-orphans` (مارمينا والبابا كيرلس) | CONTEXT §2.7، PRD §3.3، IA §1، README، AI Prompt + صف بذر جديد |
| 3 | ثلاث قوائم مختلفة لـ"14 تخصصاً" | قائمة البذر هي المصدر التنفيذي (Physio + المعمل + الصيدلية، دمج المخ والأعصاب/النفسي، دون مسالك أو تغذية) — أُلحقت بـ CONTEXT كملاحظة توافق | CONTEXT §2.4 |
| 4 | شفيع المذبح القبلي: ميكائيل (CONTEXT) مقابل مارجرجس (PRD/البذر) | **العددراء مريم والشهيد مارجرجس** | CONTEXT §2.2 |
| 5 | نموذج صلاحيات مكسور: `FOR ALL TO authenticated USING (TRUE)` يمنح كل مستخدم مسجل كتابة كاملة | **جدول `profiles` + `staff_role_enum` + دوال `is_staff()`/`is_admin()`** وجميع سياسات الكتابة مفحوصة بالدور + Bootstrap لأول مدير بـ SQL فقط | BACKEND §2/§5، ADR تعديل |
| 6 | خلل تأكيد الحجز: `.select().single()` كـ anon مع عدم وجود سياسة SELECT عامة → فشل ظاهري بعد نجاح الإدراج | الإدراج عبر **عميل service-role على الخادم** + سياسات anon INSERT تبقى كخط دفاع ثانٍ | BACKEND §7.3 |
| 7 | صفحة `/condolence/track` بلا مسار بيانات | **RPC آمن `track_condolence_booking(ref)`** (SECURITY DEFINER) يرجع حالات محدودة فقط، بلا SELECT عام على الجدول | BACKEND §3.3/§7.3 |
| 8 | أسماء وسوم إعادة التحقق متضاربة (`clinic-schedule` مقابل `clinic_absences`) | **سجل وسوم موحد** `src/lib/tags.ts` (14 وسماً) — ADR مُحدّث للإشارة إليه | BACKEND §6، ADR تعديل |
| 9 | مسار القراءة غير معرّف أصلاً (كيف تقرأ الصفحات؟) | قسم **مسار القراءة** كامل: استعلامات `unstable_cache` موسومة + `revalidate` لكل صفحة حسب مصفوفة IA | BACKEND §6 |
| 10 | نماذج PRD بلا خلفية (التسجيلات، التوظيف، اشتراكات التنبيهات) | جداول **`program_applications`** و**`job_applications`** و**`clinic_alert_subscriptions`** + مخططات Zod + إجراءات + سياسات INSERT عامة | BACKEND §3.3/§5.3/§7 |
| 11 | القداسات الموسمية والأعياد لا يمكن نمذجتها (الجدول أسبوعي فقط) | جدولا **`mass_exceptions`** (إلغاء/إضافة/تعديل ليوم محدد) و**`site_alerts`** (شريط التنبيهات المركزي) | BACKEND §3.3 |
| 12 | حالة البث المباشر غير محددة الآلية | جدول **`stream_events`** بتبديل يدوي من اللوحة (تكامل YouTube API اختياري Phase 2) + ISR 60s | BACKEND §3.3/§8 |
| 13 | قارئ الكتاب المقدس بلا مواصفة بيانات | جدولا **`bible_books`/`bible_verses`** + دالة تطبيع عربي + عمود مولّد + **RPC `search_bible`** (46+27 سفراً وفق الكانون القبطي) | BACKEND §3.3 |
| 14 | الحماية من الروبوتات مذكورة بلا تنفيذ | **Cloudflare Turnstile** عبر `verifyTurnstile()` + حد طلبات لكل IP + قائمة متغيرات البيئة | BACKEND §7.2/§9 |
| 15 | لا يوجد منع تعارض حجز القاعة | **فهرس فريد جزئي** `uq_condolence_active_date` على `event_date WHERE status IN (pending, approved)` + رسالة تعارض واضحة | BACKEND §4/§7.3 |
| 16 | بيانات البذر "الرسمية" (حسابات بنكية/كهنة/هواتف) غير قابلة للتحقق آلياً | **تحذير إلزامي في رأس البذر**: تُعامل كـ PLACEHOLDER ويشترط اعتماد رسمي قبل النشر (خطر الاحتيال المالي بأرقام IBAN خاطئة) + خانات `PLACEHOLDER` لخدمات بلا أمين معلوم | AI Prompt §4، BACKEND §9.3 |
| 17 | سطر بذر الاجتماعات 10 صفوف فقط | إضافة صف **`widows-orphans`** (آية: مزمور 67: 6 — تُتحقق من الإدارة) | AI Prompt §4 |
| 18 | مدرسة الكتاب المقدس للأطفال غائبة عن بذر المدارس (4 بدل 5) | إضافة صف **`children-bible`** | AI Prompt §4 |
| 19 | عمود `ticket_price_egp` يخالف الثابت اللفظي في CONTEXT §3 | إعادة تسمية إلى **`consultation_fee_egp`** | BACKEND §3.2، CONTEXT §2.5، AI Prompt بذر |
| 20 | أسماء مكونات متضاربة بين مصفوفة IA وشجرة الملفات | توحيد شجرة الملفات على أسماء المصفوفة (HeroBanner, NextMassCountdown, DoctorAlertTicker, QuickServiceGrid, LatestNewsCarousel, BibleVerseDaily, MassScheduleTable, RealtimeAbsenceStrip, CondolenceBookingForm) | AI Prompt §3 |
| 21 | اختلاف مواعيد/قاعات الاجتماعات بين PRD والبذر | البذر هو المرجع: ملائكة 8:30-10:30، ثانوي بالقاعة الكبرى للقديسين، الأسرة المقدسة بصحن الكنيسة | PRD §3.3 |
| 22 | "39 سفراً + أسفار ثانية" صياغة بروتستانتية | **46 سفراً للعهد القديم وفق الكانون القبطي** + 27 للجديد | PRD §3.7.3 |
| 23 | مؤشر KPI "خفض 100%" غير قابل للقياس | إعادة صياغة قابلة للقياس: أقل من 5% بمسح ربع سنوي | PRD §1.3 |
| 24 | الذهبي `#C5A880` على أبيض يخالف معيار التباين المطلوب ذاتياً | **قاعدة تصميم إلزامية**: الذهبي زخرفة فقط — النصوص الذهبية بـ `copticGold-700` | AI Prompt §2.3، PRD (تصحيح 7:1) |
| 25 | `memory-bank/` مطلوب قراءته ولا وجود له + شجرة README تتجاهل طبقة عمليات الوكلاء | **تهيئة ملفات بنك الذاكرة الستة** + تحديث شجرة المستودع وفهرس الوثائق | memory-bank/*، README |

## 2. ملفات جديدة
- `CHANGELOG_v1.1.md` (هذا الملف)
- `memory-bank/`: `projectbrief.md`, `productContext.md`, `systemPatterns.md`, `techContext.md`, `activeContext.md`, `progress.md`
- `BACKEND_AND_DATA_SPEC.md` أعيدت كتابته بالإصدار 1.1.0 (أقسام جديدة: §2 الأدوار، §3.3 الجداول الجديدة، §6 مسار القراءة، §9 الأمان التشغيلي)

## 3. ما لم يتغير (عمداً)
- `AGENTS.md` و`CLAUDE.md` (مطابقان بالأصل — حافظا على التزامن)، `docs/agents/*`، بروتوكول التتبع والوسوم، الهوية البصرية والتوكنات، مصفوفة المسارات الـ 45، وثابت INV-01 بلا أي تخفيف.
